# Quartz v4

npx quartz build --serve

npx quartz build --serve --port 8081
npx quartz build --serve --port 8081 --wsPort 3002


quartz/google-sheets-apps-script.gs é o script do Google Sheets que valida e salva as submissões.

quartz/components/scripts/submission.inline.ts é o script que recebe o SCRIPT_URL do Google Sheets
